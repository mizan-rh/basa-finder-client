import RentalListingsPage from "@/app/(MainLayout)/listings/page";

const ListingsPage = () => {
  return (
    <div>
      {/* Listings Page */}
      <RentalListingsPage />
    </div>
  );
};

export default ListingsPage;
